<package>
  <description brief="kinect">

     The Kinect nodelet based on NITE.

  </description>
  <author>Social Robotics Lab</author>
  <license>BSD</license>
  <review status="unreviewed" notes=""/>
  <url>http://ros.org/wiki/kinect</url>

  <!-- ROS packages dependencies -->
  <depend package="roscpp"/>
  <depend package="rospy"/>
  <depend package="opencv2"/>
  <depend package="sensor_msgs"/>
  <depend package="image_transport"/>
  <depend package="cv_bridge"/>

  <depend package="image_geometry"/>
  <depend package="nodelet"/>
  <depend package="rosbag" />
  <depend package="rosunit" />
  <depend package="tf" />
  <!--<depend package="rostest" /> -->

  <!-- AD packages dependencies -->
  <export>
      <rosdoc config="rosdoc.yaml" />
      <!-- nodelets -->
      <nodelet plugin="${prefix}/nodelets.xml" />
      <cpp cflags="-I${prefix} -I${prefix}/build -I${prefix}/msg_gen/cpp/include"
           lflags="-L${prefix}/lib -Wl,-rpath,${prefix}/lib"/>
  </export>
</package>


